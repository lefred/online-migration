Some ideas:
-----------

1. init_sysdb -> create a new db for table migration:

  -> create schema : online_migration
  -> create table  : migration_sys ( id, db, version, creation_date, apply_data, comment, md5sum )

    ---> md5sum is the checksum of desc of all tables
2. init <schema>

  -> make a record in online_migration.migration_sys

3. make <schema> alter.sql

  -> create a new migration
  
    a. create   file : migration/XXX_up.sql
    b. generate undo : migration/XXX_down.sql
    c. create   file : migration/XXX_meta.txt
            creation date
            comment
            md5sum ?
 
4. status <schema>

  return ID, version, applied at, description

5. migrate up <#>

6. migrate down <#>
